#include <string>
#include <iostream>

void connect_to_server(const std::string& server_address, int port) {
    // Code to connect to the server
    std::cout << "Connecting to server at " << server_address << ":" << port << std::endl;
}